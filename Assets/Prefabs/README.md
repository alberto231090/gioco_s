Cartella dove mettere i prefab creati in Unity.

Linee guida:
- Nome prefab: `tipo_descrizione.prefab` (es. `child_3y.prefab`, `sofa_glam.prefab`).
- Aggiungere tag `Child` ai prefab che rappresentano bambini.
- Per ogni prefab crea un file README con la fonte dell'asset e la licenza.